/**
 * Fallback price constant used when pricing calculations fail or inputs are invalid.
 * Value: 100,000,000 cents = $1,000,000.00
 * This high value ensures that invalid pricing is obvious in the UI/system.
 */
export const ONE_MILLION_IN_CENTS = 1_000_000_00;
